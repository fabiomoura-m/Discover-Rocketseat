// Operador de String (String operator)

// comparison (comparação)
console.log('a' == 'a')

// concatenation (concatenação)
let alpha = 'alpha'
console.log(alpha + 'bets')

let alpha1 = 'alpha'
alpha1 += 'bet'
console.log(alpha1 + 's')
