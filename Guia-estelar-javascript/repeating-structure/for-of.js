// Estrutura de Repetição

// for...of

let name = 'Fabio'
let names = ['João', 'Paulo', 'Pedro']

for (let char of name) {
  console.log(char) // retorna todos caracteres da string separadamente
}
for (let nome of names) {
  console.log(nome) // retorna os elementos do array separadamente
}
